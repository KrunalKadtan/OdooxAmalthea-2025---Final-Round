# Minimal requirements for WorkZen HRMS Backend
# Use this if you have issues with the full requirements.txt

# Django Core (Required)
Django==4.2.7
djangorestframework==3.14.0
django-cors-headers==4.3.1

# Database (Required)
psycopg2-binary==2.9.9

# Authentication (Required)
djangorestframework-simplejwt==5.3.0
PyJWT==2.8.0

# Environment Variables (Required)
python-decouple==3.8

# ML & Analytics (Optional - can be installed separately)
textblob==0.17.1

# PDF Generation (Required for payslips)
reportlab==4.0.7
Pillow>=10.0.0

# Utilities (Required)
python-dateutil==2.8.2
pytz==2023.3
